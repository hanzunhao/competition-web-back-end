spring:
  application:
    name: competition-web  # Spring Boot ???????

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  # MySQL ? JDBC ???
    url: jdbc:mysql://localhost:3306/competition-web  # ????? URL????????????????
    username: root  # ????????????????????
    password: 111111  # ??????????????????????

  servlet:
    multipart:
      max-file-size: 10MB  # ???????????
      max-request-size: 100MB  # ??????????????????????

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # MyBatis ?????????????
    map-underscore-to-camel-case: true  # ?????????????????? Java ????????

logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug  # ? JdbcTransactionManager ???????? DEBUG?????????